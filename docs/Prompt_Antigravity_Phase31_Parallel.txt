[FOR ANTIGRAVITY — GITHUB EXECUTION PROMPT]
Project: PaperPipe P2 (Pre-Feature Hardening)
Date: 2026-02-19
Mode: GitHub/PR packaging only

You own all GitHub-side actions (branching, commit split, PR creation, merge strategy).
Do not redesign code. Keep commits surgical and aligned to the scopes below.

============================================================
0) Current Intent
============================================================
- Package and ship the already-implemented hardening work.
- Preserve runtime path: `worker.py -> backend/services/job_runner.py`.
- Preserve v2 compatibility path for agents.
- Include regression guards.

============================================================
1) Required PR Scopes
============================================================
PR-A: Runtime hardening + v2 compatibility + worker chain guard
- Core files:
  - `backend/services/job_runner.py`
  - `src/jobs/worker.py`
  - `src/jobs/queue.py`
  - `src/jobs/schemas.py`
  - `src/db_utils.py`
  - `backend/main.py`
  - `src/agents/reader_agent.py`
  - `src/agents/indexer_agent.py`
  - `src/agents/stats_agent.py`
  - `src/agents/ingest_agent.py`
  - `src/contracts/document_artifact_v2.py`
  - `src/contracts/artifact_views.py`
  - `src/contracts/artifact_bridge.py`
  - `src/ingest/__init__.py`
  - `tests/test_stats_agent.py`
  - `tests/test_artifact_bridge.py`
  - `tests/test_jobs_api_smoke.py`
  - `tests/test_worker_job_runner_chain.py`

PR-B: PR packet docs / operator handoff docs
- Core files:
  - `docs/archive/P2_PreFeature_Hardening_Execution_Plan_2026-02-19.md`
  - `docs/archive/P2_PR1-PR3_Review_Packet_2026-02-19.md`
  - `docs/archive/P2_PR1-PR3_PR_Descriptions_2026-02-19.md`
  - `docs/archive/PaperPipe_Agent_Personas_Audit_2026-02-19.md`
  - `docs/Prompt_Antigravity_Phase31_Parallel.txt`

Note:
- If unrelated files are also modified in working tree, exclude them from these PRs unless strictly required by tests for this scope.

============================================================
2) Acceptance Criteria To Assert In PR Description
============================================================
For PR-A:
- `worker.py` no longer uses placeholder flow; it calls real `run_deepread_job`.
- `run_deepread_job` returns structured result (`succeeded|failed|cancelled`) and supports callback/cancel hooks.
- Ingest uses `process_v2`.
- Reader/Indexer/Stats accept `DocumentArtifactV2` input in runtime path.
- Added chain regression test proving artifact outputs are produced via worker->job_runner.
- `datetime.utcnow()` deprecation removed in job runner (`datetime.now(timezone.utc)`).

For PR-B:
- PR templates/packets include Scope, AC checklist, test commands, example artifact paths.
- Persona/agent audit doc reflects code-verified state and known risks.

============================================================
3) Minimum Test Commands (paste in PR body)
============================================================
```bash
pytest -q tests/test_worker_job_runner_chain.py tests/test_jobs_api_smoke.py
pytest -q tests/test_stats_agent.py tests/test_artifact_bridge.py
pytest -q tests/test_document_artifact_v2.py tests/test_ocr_fallback.py tests/test_eval_harness.py
```

============================================================
4) PR Body Template (use exactly this structure)
============================================================
## Scope
- ...

## AC Checklist
- [x] ...
- [x] ...

## How To Verify
```bash
...
```

## Example Artifacts
- `storage/artifacts/<paper_id>/<run_id>/document_artifact.json`
- `storage/artifacts/<paper_id>/<run_id>/index_artifact.json`
- `storage/artifacts/<paper_id>/<run_id>/claimset.json`
- `storage/artifacts/<paper_id>/<run_id>/stats_report.json`

## Risk / Non-goals
- No cloud API dependency added.
- No destructive migration beyond additive job columns.
- No refactor beyond this hardening scope.

============================================================
5) Commit Hygiene
============================================================
- Keep commit messages explicit:
  - `hardening: wire worker to real deepread runner + callback/cancel`
  - `contracts: accept documentartifact v2 across reader/indexer/stats`
  - `test: add worker->job_runner chain smoke regression`
  - `docs: add p2 pr1-pr3 submission packet and execution notes`
- Avoid mixing docs-only and runtime code in the same commit.

END
